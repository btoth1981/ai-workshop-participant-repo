STATUS: VERIFIED
SOURCE_SHA: 665bdedf2b0cb14c05c2a18439437eb345ba3d6b
ENVIRONMENT: lokális vitest + élő Neon TESZT-branch
ADAPTER: Neon Postgres (VALÓDI adapter) — br-proud-glitter-ats5mp18; az aktív
         forrás azonosítása: DATABASE_URL shell-env a branch pooled endpointjára
         (ep-misty-field-att9hwxt-pooler...), NEM a production default branch
RUN_DATE: 2026-07-15

Megjegyzés az adapter-párhoz: a rendszerben nincs memória-/fake-adapter — a
saveOrder szándékosan vékony IO-réteg (T7 döntés), a tiszta buildOrder pedig
DB nélkül unit-tesztelt. A contract-szint így: a VALÓDI adapter írás→visszaolvasás
szerződése + DB-szintű invariáns-sértés elutasítása.

== Parancs ==
DATABASE_URL=<teszt-branch> npx vitest run src/lib/c6-contract.integration.test.ts
(átmeneti, untracked tesztfájl; futás után törölve — repóba emelése: T9 follow-up)

== 1. futás: EXIT 1 — a TESZT hibája, nem a kódé ==
AssertionError: expected undefined to be 10970 — az átmeneti teszt rossz
mezőnevet tippelt (subtotal vs. a valódi subtotalHuf). Javítás után:

== 2. futás: EXIT 0 ==
Test Files  1 passed (1)
Tests       2 passed (2)

1) "persists a built order and reads back identical data":
   buildOrder([szett×2, gel×1]) → subtotalHuf=10 970, shippingHuf=0 (>=10 000,
   ingyenes!), totalHuf=10 970 → saveOrder → uuid → SELECT visszaolvasás:
   orders.subtotal/shipping/total = 10970/0/10970; order_items: gel/1990/1 db,
   szett/4490/2 db — beküldéskori név+ár snapshot (E5) a DB-ben egyezik.
2) "rejects a DB-level contract violation":
   INSERT order_items quantity=0 → a DB CHECK (quantity BETWEEN 1 AND 99)
   elutasította (hibaüzenet a check constraintre hivatkozik).
